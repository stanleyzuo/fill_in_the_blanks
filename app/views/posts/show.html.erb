<h1><%= @post.title %></h1>

<p><%= @post.body %></p>

<p>Submitted by: <%= @post.bru_name %> </p>

<%= link_to 'Edit', edit_post_path(@post) %> |

<%= link_to 'Delete', post_path(@post),
              method: :delete,
              data: { confirm: 'Are you sure?' } %>

<p><%= link_to 'All Posts', posts_path %></p>